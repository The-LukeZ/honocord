These tests are supposed to be typechecked only. They are not executed. They exist to ensure that the types exposed by the library work as intended.

Run with `tsc src/test --noEmit` from the project root (or use the test script).
